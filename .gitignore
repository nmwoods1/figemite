node_modules/
dist/
coverage/
# local board data — never versioned
boards/
# static build output
public/
.history/
*.tsbuildinfo
.DS_Store
.env*
# npm auth — never versioned (a project .npmrc may hold a publish token)
.npmrc
# session tooling — never versioned
.claude/
.playwright-mcp/
# Playwright e2e output (P3-T21) — reports/traces/screenshots from local or CI runs
/packages/client/test-results/
/packages/client/playwright-report/
/packages/client/blob-report/

# git worktrees (isolated dev workspaces)
.worktrees/
